# Network linux tools

## `iptables`

`sudo iptables --list-rules` can show firewall configs

## `traceroute`

`traceroute <domain name>` can show how a request is routed to destination.

## `lsof -i -P -n`

`lsof -i -P -n` can show port usage